name = "hhb-d1-api"
main = "workers/d1-api.js"
compatibility_date = "2025-11-11"

# --- Globale Einstellungen ---

# Fülle hier später deine Cloudflare Account ID ein:
# account_id = "DEINE_ACCOUNT_ID"

[build]
command = ""

# --- Umgebungsspezifisch: Production ---

[env.production]

# WICHTIG: Deine Secrets (OpenAI, ElevenLabs, AWS) bleiben
# im Cloudflare Dashboard gespeichert!


# D1 Database binding (kopiert aus workers/wrangler.toml)
# Ensure the database binding is defined for the production environment.
# Use the array form so it's clear this belongs to env.production.
d1_databases = [
	{ binding = "DB", database_name = "db-elite-pro", database_id = "fdf88b02-08d3-4d78-a6c3-d6dc36f4fac6" }
]


