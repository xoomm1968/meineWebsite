# Example env file for HHB backend scaffold
# Copy to .env and fill in keys (do NOT commit real keys to git)

NODE_ENV=development
PORT=6789
# Provider API keys (optional)
OPENAI_API_KEY=
ELEVENLABS_API_KEY=
AWS_ACCESS_KEY_ID=
AWS_SECRET_ACCESS_KEY=

# Optional application secret for simple API auth
APP_API_TOKEN=replace_this_with_a_secret
